- name: Installing stow
  shell: brew install stow
  tags:
    - install
    - dotfiles
    - stow
- name: Cloning .dotfiles
  shell: cd $HOME && git clone https://github.com/wesbragagt/.dotfiles.git
  tags:
    - install
    - dotfiles
    - stow
- name: Stow dotfiles
  shell: cd $HOME/.dotfiles && ./setup.sh
  tags:
    - install
    - dotfiles
    - stow
